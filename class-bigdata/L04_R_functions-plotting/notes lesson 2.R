#dopo che ho scritto un codice ho bisogno di un dataset per testare il codice, questo è chiamato debugging
#posso mettere in una variabile una funzione e definire gli argomenti
#newvector dice che non esiste perchè è solo parte dell'environment dentro la funzione e non nel nostro globale