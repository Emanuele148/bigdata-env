#comment
#vectors can contain only one type of character num or chr for example
#parentesi quadra singola ti ridà un oggetto con lo stesso formato dal quale l'elemento è stato sliceato mentre le doppie parentesi ti da un formato adatto a quello che hai sliceato (es se prendo il secondo elemento da una lista con le doppie parentesi non mi darà una lista ma un vettore)
#nei dataframe si ragiona sempre in colonne. Quando printo il dataframe prima dell'uguale dico il nome delle colonne
#nei datafrme visto che ragionano per colonne, se voglio fare uno slice, non devo specificare le 2 dimensioni come nelle matrix 
#traformo il mio dataframe in un tibble 
#ctrl+shift+m per fare: %>%, è una pipe permette di combianare più operazioni insieme, prende quello che è stato fatto prima e lo cambia in quello che c'è prima